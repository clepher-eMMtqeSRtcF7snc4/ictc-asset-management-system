import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import type { TransferAsset, NewAssignment } from "./types";
export function TransferDetailsForm({ asset, value, onChange }: { asset: TransferAsset; value: NewAssignment; onChange: (value: NewAssignment) => void }) { const update = (key: keyof NewAssignment, next: string) => onChange({ ...value, [key]: next }); return <section><h2 className="text-base font-semibold">2. Transfer Details</h2><p className="mb-4 text-xs text-muted-foreground">Compare the current assignment and prepare the new destination.</p><div className="grid gap-4 md:grid-cols-2"><AssignmentBox title="Current Assignment" rows={[["Custodian", asset.custodian], ["Department", asset.department], ["Office", asset.office], ["Location", asset.location]]} /><div className="rounded-md border border-primary/30 bg-primary/5 p-4"><p className="mb-3 font-semibold text-primary">New Assignment</p><div className="grid gap-3"><Field label="New Custodian *"><Select value={value.custodian} onValueChange={(next) => update("custodian", next)}><SelectTrigger><SelectValue placeholder="Select active custodian" /></SelectTrigger><SelectContent><SelectItem value="Maria Santos">Maria Santos · EMP-00141</SelectItem><SelectItem value="Ana Reyes">Ana Reyes · EMP-00163</SelectItem></SelectContent></Select></Field><Field label="New Department *"><Select value={value.department} onValueChange={(next) => update("department", next)}><SelectTrigger><SelectValue placeholder="Select department" /></SelectTrigger><SelectContent><SelectItem value="Engineering">Engineering</SelectItem><SelectItem value="Accounting">Accounting</SelectItem><SelectItem value="ICT">ICT</SelectItem></SelectContent></Select></Field><Field label="New Office *"><Select value={value.office} onValueChange={(next) => update("office", next)}><SelectTrigger><SelectValue placeholder="Select office" /></SelectTrigger><SelectContent><SelectItem value="Engineering Office">Engineering Office</SelectItem><SelectItem value="Accounting Office">Accounting Office</SelectItem></SelectContent></Select></Field><Field label="New Location *"><Select value={value.location} onValueChange={(next) => update("location", next)}><SelectTrigger><SelectValue placeholder="Select location" /></SelectTrigger><SelectContent><SelectItem value="Engineering - Room 305">Engineering - Room 305</SelectItem><SelectItem value="Main Building - Room 110">Main Building - Room 110</SelectItem></SelectContent></Select></Field></div></div></div><div className="mt-4 grid gap-3 md:grid-cols-2"><Field label="Transfer Date *"><Input type="date" value={value.transferDate} onChange={(event) => update("transferDate", event.target.value)} /></Field><Field label="Transfer Reason *"><Select value={value.reason} onValueChange={(next) => update("reason", next)}><SelectTrigger><SelectValue placeholder="Select reason" /></SelectTrigger><SelectContent><SelectItem value="Staff reassignment">Staff reassignment</SelectItem><SelectItem value="Office relocation">Office relocation</SelectItem><SelectItem value="Department transfer">Department transfer</SelectItem></SelectContent></Select></Field><Field label="Remarks"><Textarea value={value.remarks} onChange={(event) => update("remarks", event.target.value)} placeholder="Optional transfer notes" /></Field></div></section>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="grid gap-1.5 text-xs font-medium">{label}{children}</label>; }
function AssignmentBox({ title, rows }: { title: string; rows: string[][] }) { return <div className="rounded-md border bg-muted/30 p-4"><p className="mb-3 font-semibold">{title}</p><dl className="space-y-2 text-sm">{rows.map(([label, value]) => <div key={label} className="flex justify-between gap-3"><dt className="text-muted-foreground">{label}</dt><dd className="font-medium text-right">{value}</dd></div>)}</dl></div>; }
