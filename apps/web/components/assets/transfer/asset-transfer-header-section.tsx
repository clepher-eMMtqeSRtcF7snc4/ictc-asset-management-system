import { ArrowRightLeft } from "lucide-react";
import { Button } from "@/components/ui/button";
export function AssetTransferHeaderSection({ enabled, onReview, onCancel }: { enabled: boolean; onReview: () => void; onCancel: () => void }) { return <header className="flex flex-col gap-4 border-b pb-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs text-muted-foreground">Dashboard <span className="mx-2">›</span> Asset Management <span className="mx-2">›</span> Asset Transfer</p><h1 className="mt-3 flex items-center gap-2 text-2xl font-bold"><ArrowRightLeft className="size-6 text-primary" />Asset Transfer</h1><p className="mt-1 text-sm text-muted-foreground">Transfer an assigned ICT asset to a new custodian, department, office, or location.</p></div><div className="flex gap-2"><Button variant="outline" onClick={onCancel}>Cancel</Button><Button disabled={!enabled} onClick={onReview}>Review & Transfer</Button></div></header>; }
