import { PageHeader } from "@/components/layout/page-header"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Download, FileBarChart2, Printer } from "lucide-react"

export function ReportsPage() { return <div className="space-y-6"><PageHeader title="Reports Center" description="Generate filtered operational, valuation, maintenance, and compliance reports." /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{[["Asset Registry", "Full asset register by status, department, and location"], ["Current Stock", "Inventory availability and low-stock replenishment"], ["Maintenance Cost", "Work orders, costs, and frequent issues"], ["Depreciation Summary", "Book value and fully depreciated equipment"], ["Disposed Assets", "Disposal status, reason, and method"], ["Procurement Status", "Purchase request and receiving progress"]].map(([title, detail]) => <Card key={title}><CardHeader><CardTitle className="flex items-center gap-2 text-base"><FileBarChart2 className="size-4 text-primary" /> {title}</CardTitle></CardHeader><CardContent><p className="min-h-10 text-sm text-muted-foreground">{detail}</p><div className="mt-4 flex gap-2"><Button variant="outline" size="sm"><Printer /> Preview</Button><Button size="sm"><Download /> Export</Button></div></CardContent></Card>)}</div></div> }
